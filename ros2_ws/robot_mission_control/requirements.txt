# [CHANGE | 2026-04-24 10:48 UTC | v0.201]
# CO ZMIENIONO: `requirements.txt` ustawiono jako profil headless (core/ROS) bez GUI.
# DLACZEGO: Backendowe testy CI muszą działać bez PySide6 i bez sztucznych obejść środowiska.
# JAK TO DZIAŁA: Plik deleguje do `requirements-core.txt`, a GUI instalujemy osobno z `requirements-ui.txt`.
# TODO: Dodać skrypt bootstrapu, który automatycznie wybiera profil `core` lub `ui` zależnie od roli środowiska.
-r requirements-core.txt
