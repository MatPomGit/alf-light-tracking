; [AI-CHANGE | 2026-04-29 13:15 UTC | v0.332]
; CO ZMIENIONO: Oznaczono `setup.cfg` jako konfigurację zgodności dla instalacji setuptools poza buildem ROS2.
; DLACZEGO: `ros2 run` używa teraz wrapperów instalowanych przez CMake, ale lokalne tryby `setup.py develop/install`
;           nadal powinny kierować skrypty do `lib/robot_mission_control`.
; JAK TO DZIAŁA: Sekcje `develop` i `install` zachowują dawny katalog skryptów bez wpływu na `ament_cmake`.
; TODO: Dodać test wykrywający rozjazd nazw wrapperów między `setup.py` i `CMakeLists.txt`.
[develop]
script_dir=$base/lib/robot_mission_control

[install]
install_scripts=$base/lib/robot_mission_control
