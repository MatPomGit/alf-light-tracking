; [AI-CHANGE | 2026-04-21 12:10 UTC | v0.167]
; CO ZMIENIONO: Dodano konfigurację ścieżek skryptów instalowanych przez ament_python.
; DLACZEGO: `ros2 run` oczekuje skryptów w `lib/<package_name>`, inaczej entrypoint może nie być odnaleziony.
; JAK TO DZIAŁA: Sekcje `develop` i `install` ustawiają katalog docelowy skryptów na `lib/robot_mission_control`.
; TODO: Rozważyć dodanie walidacji setup.cfg w pre-commit (np. check-manifest + custom lint).
[develop]
script_dir=$base/lib/robot_mission_control

[install]
install_scripts=$base/lib/robot_mission_control
